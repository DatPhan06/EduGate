import React from 'react';
import { Box, Typography } from '@mui/material';

const Events = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4" gutterBottom>
                Quản lý Lịch sự kiện & Thời khóa biểu
            </Typography>
            <Typography>
                Module quản lý lịch sự kiện và thời khóa biểu sẽ được phát triển tại đây.
            </Typography>
        </Box>
    );
};

export default Events; 